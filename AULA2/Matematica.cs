using System;
using System.Globalization;

public class Matematica
{
    double  x = 3.0;
    double  y = 4.0;
    double z = -5.0;
    double a,b,c;

    a = Math.sqrt(x);
    b = Math.sqrt(y);
    c = Math.sqrt(25.0);
}